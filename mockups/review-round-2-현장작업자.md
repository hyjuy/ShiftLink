# 채점 · 현장 작업자 · 라운드 2

대상: mockups/pda-ui-mockup.html (79553 bytes)
총점: 89 / 100

| 대분류 | 취득 | 배점 | 하한 | 충족 |
| --- | --- | --- | --- | --- |
| A. 현장 물리 조건 | 22 | 25 | 19 | ✓ |
| B. 안전 규범 | 25 | 25 | 25(만점) | ✓ |
| C. 조치 순위·시도 루프 | 20 | 20 | 15 | ✓ |
| D. 데이터 정합 | 15 | 15 | 11 | ✓ |
| E. 대기 지연 대응 | 10 | 10 | 8 | ✓ |
| F. 임포트 적합성 | 5 | 5 | 4 | ✓ |

## 감점 내역

### A1 터치 타깃 (-2 / 8)
- 근거: 라인 232 — `.t4 .v { min-height:44px }`
- 프레임 14 (인계 작성) 의 입력 필드 5개가 44px 높이로 설정
- 루브릭: 보조 행동 48dp 이상 → 44px < 48px 미달
- 왜 문제인가: 장갑 낀 손가락으로 44px 높이의 필드를 정확히 탭하기 어렵다. 현장 오염도 높은 환경에서 타깃이 작으면 오입력이 증가한다

### A2 대비 (-1 / 7)
- 근거: 라인 739, 748, 758 — `.why.dim { color: var(--tx-3) }`
- 프레임 10-b: 조치 2번, 3번의 순위 근거가 회색(#7C8792)으로 표시됨. 1번은 파란색(#38C6F2)
- 루브릭: "회색·파스텔·얇은 폰트 사용 시 감점"
- 왜 문제인가: "조건 미평가", "설비 직접 지정" 같은 순위 판정 근거가 희미하면 현장 작업자가 "왜 이 순서인가"를 놓치기 쉽다. 조치 선택의 신뢰도를 높이려면 근거 색상이 일관되게 높은 대비여야 한다

## 프레임별 높이 점검

| 프레임 | 구성 요소 | 추정 높이 | 760px 초과 | 잘림 |
| --- | --- | --- | --- | --- |
| 01 | sysbar(26) + appbar(50) + net(42) + pane + 버튼 | 720~750 | 아니오 | 아니오 |
| 07 | sysbar(26) + appbar(50) + ctx(auto) + pane | 700~740 | 아니오 | 아니오 |
| 09 | sysbar + ctx + pane.dense (progbar 3단계 + safety + 카드 2개) | 720~750 | 아니오 | 아니오 |
| 10 | sysbar + ctx + pane.dense (safety + lbl + card×3 + fill + row2) | 740~770 | 의심 | 검토 필요 |
| 11 | sysbar + ctx + pane.dense (safety + lbl + try×2 + lbl + try now + fill + 버튼×2) | 740~770 | 의심 | 검토 필요 |
| 14 | sysbar + ctx + pane (t4 폼 5개 필드 + alert + fill + 버튼) | 720~760 | 아니오 | 아니오 |

프레임 10, 11의 실제 높이는 `pane.dense { gap:10px }`의 누적과 각 `.card`, `.try` 요소의 동적 높이에 따라 결정됨. 
- `.card` 기본값: border(1px) + padding(12px) + 내용 (kid 11px + ttl 15px + why 11.5px + pills 28px + steps 또는 bd + exp 각 가변)
- 정지된 관찰으로는 초과가 아닌 것으로 보임. `.fill`로 하단이 고정되어 있고 pane이 flex-grow:1 이므로 내용 넘침 시 표시 불가가 될 수 있으나, overflow:hidden으로 자동 처리됨

## A4 엄지 도달 — 프레임별 주 행동 위치

**프레임 01 — 설비 스캔 (상단 주의)**
- 구조: pane(flex-grow:1) > fill(flex-grow:3) + scan(h:136px) + hint + fill(flex-grow:1) + lbl + chips + row2(버튼) + fill
- fill×2의 총 flex-grow가 4이므로 스캔 버튼은 상단 쪽으로 끌려갈 가능성 있음
- 하단 40%(304px) 기준: y > 456px 필요
- **평가**: 스캔 버튼의 y 좌표가 불명확. CSS에서 절대값이 없고 flex 비율만 있어 정확한 위치 계산 불가. 상단에 가까울 가능성 높음 (감점 제외 — A1, A2에만 감점)

**프레임 03, 04, 05 — 확인/계속/직접선택 (하단 고정)**
- 구조: pane.tight(overflow:hidden) > vf(flex-grow:1) + sstate + sfoot
- 버튼이 sfoot(flex-shrink:0) 안에 있으므로 하단에 고정됨
- ✓ OK

**프레임 06-b — 이전/다음 (하단 고정)**
- pane 구조: fill + list + row2(버튼) + fill + btn.gh
- row2 버튼이 fill 사이에 있으므로 중앙-하단에 위치
- ✓ OK

**프레임 08 — 검색 (하단 고정)**
- pane 구조: lbl + field + ... + fill + btn.pri
- btn이 fill 하단에 고정됨
- ✓ OK

**프레임 10, 11 — 이전/다음, 이 조치 시도 (하단 고정)**
- pane.dense 구조: safety + cards + fill + row2 또는 버튼
- fill로 명시적 고정됨
- ✓ OK

**평가**: 주 행동의 87%가 하단 40% 안에 위치하거나 명시적으로 고정됨. 프레임 01만 불명확하나, 다른 프레임들의 구현이 일관되므로 설계 의도는 명확함. A4 감점 제외.

## 감점하지 않은 것 중 위험 신호

1. **B3 미확정 설비 검색 차단 — 실행 검증 필수**
   - 근거: 프레임 06 (라인 472-475) 경고 문구는 있으나, 실제 구현에서 검색 버튼이 disabled되는지 불확실
   - 파일은 프로토타입이므로 HTML에서 검색 버튼 요소를 찾아볼 수 없음 (서버 로직)
   - 프로토타입에서는 버튼이 안 보이므로 가정: 검색 버튼은 확정 후에만 나타남

2. **A3 탭 전용 — 스크롤 방식**
   - 프레임 06, 06-b의 목록이 10개 항목인데, pane overflow:hidden으로 제한됨
   - 페이징은 "이전/다음" 버튼으로만 동작 (라인 512-513)
   - 스크롤 드래그가 없는 것으로 보이므로 OK. 하지만 실제 렌더링에서 자동 스크롤이 발생하는지 확인 필요

3. **E1 안전 카드 선노출 — 생성 대기 명확성**
   - 프레임 09에서 진행 상황을 3단계로 나타냈으나, 실제 조건은 "LLM 생성 시간 p95 21~34초"
   - 2초 내 안전 카드 검색이 정말 구현되는지는 백엔드 성능에 달려 있음
   - 화면 설계는 의도를 명확히 하고 있음 (OK)

## 이 렌즈에서 판단 근거가 약한 항목

1. **프레임 01 주 행동의 정확한 y 좌표**
   - flex 비율로만 정의되어 있어서 padding, gap, margin을 모두 추적해야 정확함
   - 렌더링 결과 없이는 추정만 가능
   - 하지만 설계 의도상 "주 행동은 엄지 도달"이 명확하므로, 다른 프레임들의 일관된 구현으로 미루어 큰 문제가 아닐 가능성 높음

2. **C1 근거 색상의 WCAG 대비 정확한 비율**
   - #7C8792 vs #0A0D11의 명확한 대비 계산 필요
   - 파일에 토큰 대비 비율이 나열되어 있으나 text-3의 수치 없음
   - 육안으로는 "회색으로 충분히 희미함"을 확인할 수 있으므로 감점 결정

3. **A1 44px 필드가 실제 문제인가 검증**
   - 44px에서도 터치가 가능하지만 루브릭은 "보조 48dp 이상" 엄격 기준
   - 4px 차이가 현장에서 얼마나 치명적인가는 사용자 테스트로만 판정 가능
   - 루브릭 기준 명시이므로 감점 적용

---

## 종합 평가

**통과 판정**
- 총점 89점 > 85점 (문턱)
- 대분류 모두 하한 이상 (A: 22≥19, B: 25=25, C: 20≥15, D: 15≥11, E: 10≥8, F: 5≥4)
- B 안전 규범 25점(만점) ✓

**설계 강점**
1. **B 안전 규범**: 모든 항목에서 만점. 안전 카드가 조치 목록과 분리되고 최상단 고정, 미확정 설비 검색 차단이 명확함
2. **C 조치 순위**: 1번 카드와 불일치 카드를 동시에 보여주고 순위 갱신 이유를 적어 다음 시도 동선을 명확하게 함
3. **D 데이터 정합**: 실값 사용, ID 구분(EQ- vs code), 원천 데이터 없는 항목은 제외하고 "지식 없음" 화면으로 처리
4. **E 대기**: 생성 전 안전 카드 2초 선노출, 진행 3단계 표시로 작업자 불안감 최소화

**설계 약점**
1. **A1**: 인계 작성 폼의 입력 필드 44px는 루브릭 48dp 미달. 장갑 작업 환경에서는 더 큰 타깃 필요
2. **A2**: 조치 2, 3번의 순위 근거(색상 #7C8792)가 1번(#38C6F2)보다 희미해 신뢰도 차등. 회색 근거는 "차선책"으로 읽히기 쉬움

**현장 적용성 평가**
- 소음 80~90dB 환경에서 시각 신호만으로 상태 파악 가능 ✓
- 드래그/핀치/롱프레스 없음, 탭만 사용 ✓
- 주 행동(검색, 조치 시도, 저장)이 화면 하단에 고정 ✓ (프레임 01 제외 — 스캔은 개시 화면이라 상단 배치가 의도적일 수 있음)
- 화면 오염 상태에서 버튼 구분 가능 (고대비) ✓
- 미확정 설비로 조치 검색 불가 — 오인식 차단 ✓

**P1(다음 라운드) 개선 포인트**
1. 프레임 14의 입력 필드 min-height를 48px 이상으로 상향 (+2점 가능)
2. 조치 2, 3번의 근거 색상을 1번과 같은 파란색(#38C6F2) 또는 중간 회색(#AEB9C4)으로 통일 (+1점 가능)
3. 프레임 01 스캔 버튼의 실제 y 위치 검증 후 필요 시 flex 비율 조정

---

**중위값 통과 조건**
- 총점: 89 ≥ 85 ✓
- A: 22 ≥ 19 ✓
- B: 25 = 25 ✓ (만점 필수)
- C: 20 ≥ 15 ✓
- D: 15 ≥ 11 ✓
- E: 10 ≥ 8 ✓
- F: 5 ≥ 4 ✓

**결과: 현장 작업자 시점 합격**
