# GitHub 업로드 방법 (Windows PowerShell)

작성: 2026-09-17 · 대상 저장소: https://github.com/hyjuy/ShiftLink

## 상황

클라우드 세션과 데스크톱 작업공간 양쪽 모두 GitHub 푸시가 프록시 정책에 막혀 있습니다
(`403 ... not in this session's authorized repository set`).
그래서 **본인 Windows 터미널에서 직접 푸시**하는 방식으로 준비했습니다.

저장소 규칙(`docs/branching.md`)이 `main` 직접 push를 금지하므로 브랜치를 씁니다.
브랜치명: `chore/plan-v2-draft-260917`

---

## 방법 A — bundle 사용 (권장)

커밋과 커밋 메시지가 그대로 들어갑니다. PowerShell에 그대로 붙여넣으세요.

```powershell
cd C:\
git clone https://github.com/hyjuy/ShiftLink.git shiftlink-repo
cd C:\shiftlink-repo
git fetch C:\shiftlink\shiftlink-plan-draft.bundle chore/plan-v2-draft-260917:chore/plan-v2-draft-260917
git checkout chore/plan-v2-draft-260917
git push -u origin chore/plan-v2-draft-260917
```

마지막 줄 출력에 나오는 링크를 열면 PR 생성 화면으로 갑니다.

## 방법 B — 폴더 복사

bundle이 안 되거나 직접 커밋하고 싶을 때.

```powershell
cd C:\
git clone https://github.com/hyjuy/ShiftLink.git shiftlink-repo
cd C:\shiftlink-repo
git checkout -b chore/plan-v2-draft-260917
Copy-Item -Recurse "C:\shiftlink\docs\plan-v2-draft-260917" "C:\shiftlink-repo\docs\"
git add docs/plan-v2-draft-260917
git commit -m "docs: 계획서 고도화 초안 및 검토 결과 추가 (미승인)"
git push -u origin chore/plan-v2-draft-260917
```

---

## PR 본문 (복사해서 붙여넣기)

제목:
```
docs: 계획서 고도화 초안 및 검토 결과 (미승인, 검토 요청)
```

본문:
```
2026-09-17 고도화 요구("개별 장비 관점 → 생산시스템 전체 관점", 결과 관리·매뉴얼 개정
범위 추가)를 기존 계획서 v1.1과 대조한 검토용 초안입니다.

**이 PR은 계획을 확정하지 않습니다.** 기존 승인 항목(#1~#25)은 하나도 변경하지 않았고,
변경 제안은 02 변경표와 03 결정질문에만 기재했습니다. 노션도 수정하지 않았습니다.

## 추가 내용
docs/plan-v2-draft-260917/
- README.md — 미승인 초안임을 밝히고 읽는 순서 안내
- 01 고도화 초안 (이것만 읽어도 검토 가능)
- 02 변경표 65행 — 등급 A(승인변경 필요) 6 / B(미결 결정) 15 / C(신규) 39 / D(문안) 5
- 03 결정질문 24건 — T1(0주차 필수) 4건 포함
- 04 확정·미결 목록
- 05 정합성 검수 결과 — 별도 검수자가 찾은 위반 30건과 반영 내역
- annex/ — 전문 검토 3건 + 대표 사건 3건 JSON 인스턴스

## 먼저 답이 필요한 4개 (0주차, ~9/28)
- **Q1** 신규 범위의 절단선 — 전부 넣으면 4주에 완료되지 않는다는 판단이 있음
- **Q2** 데이터 규모 — [승인 #5] 유지 vs 하향. 검토자 3인의 추천이 갈렸음
- **Q3** 매뉴얼 개정·승인·발행을 MVP에 넣는가 — 기존안에 없는 기능
- **Q4** 도구 5종 유지 vs 7종 — [승인 #21] 변경 여부

## 주장하지 않는 것
- Jetson 실측·저장소 코드 검사 미수행. 수치는 전부 [미측정]/[가정]/[제안값]/[가상 값]
- MVP 최고 검증 등급 L1. 이 검토는 팀 검수도 전문가 검증도 아니며 L2가 아님
- 4주 안에 신규 전 범위가 완료된다고 주장하지 않음
```

---

## 참고

- `C:\shiftlink` 최상위에 흩어져 있는 md·json 파일들은 앞서 받으신 원본입니다.
  이번에 정리한 `C:\shiftlink\docs\plan-v2-draft-260917\` 안에 같은 내용이 들어 있으니,
  최상위 파일들은 지우거나 두셔도 됩니다. (제가 임의로 지우지 않았습니다)
- `constraint_register.md`는 이번 작업과 무관한 이전 파일이라 건드리지 않았습니다.
